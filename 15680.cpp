#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(0);
  cout.tie(0);
  ios::sync_with_stdio(0);
  
  int a;
  cin >> a;

  cout << (a ? "Leading the Way to the Future" : "YONSEI");

  return 0;
}
