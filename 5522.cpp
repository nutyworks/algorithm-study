#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(0);
  cout.tie(0);
  ios::sync_with_stdio(0);
  
  int a, b, c, d, e;
  cin >> a >> b >> c >> d >> e;
  cout << a + b + c + d + e;

  return 0;
}
